{
  "name": "@ta/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Drizzle 스키마 및 커넥션 — primaryDb / replicaDb 분리 (INV-12)",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:coverage": "vitest run --coverage --passWithNoTests"
  },
  "dependencies": {
    "@ta/contracts": "workspace:*",
    "@ta/domain": "workspace:*",
    "drizzle-orm": "^0.38.0",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
